# Mechanism Round — thoughts (decision log)

- codex ran full Part B inside its own session after validations — fine,
  ledger units are the source of truth either way.
- A2's inverted-U tempted an immediate "cap the tail" change; held the line:
  ran it as labeled-exploratory M6 instead of adopting. It failed (-97pp),
  which retroactively justifies the discipline — the unconditional 5d event
  stat does not survive contact with the full rule stack (frequency halved,
  good extreme trades thrown out with the bad).
- The strongest result of the round is negative-space: five independent,
  mechanism-motivated deformations all lose big (-72..-130pp). That is what
  a local optimum looks like from the inside; the constructive findings are
  the Part-A mechanism shapes that explain WHY the incumbent rules win.
